#include "catch.hpp"
#include "Date.h"

TEST_CASE( "Date", "[Date]" ) {
    //Date refDate = Date(2017,10,27,12,0,0);

    SECTION("Less than"){
        //Date date1 = Date(2017,10,27,11,59,59);
        //REQUIRE(date1 < refDate);
    }
}
